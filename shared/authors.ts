/**
 * Content author for editorial pages. Content is attributed to the company
 * (no named individual), so the Article schema emits an Organization author.
 */
export const CONTENT_AUTHOR = {
  name: 'Boise Handyman Co',
  url: '/about',
} as const;
